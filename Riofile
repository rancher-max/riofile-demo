services:
  custrev:
    env:
    - FOO=${REVISION}
    - BAR=BAZ
    - BAZ=${NAMESPACE}
    build:
      branch: master
      buildContext: .
      dockerfile: Dockerfile
      repo: https://github.com/rancher-max/rio-demo
      imageName: myname/custimage
    ports:
    - 8080
  custlocal:
    build:
      buildContext: ./blah/
      dockerfile: ./blah/Dockerfile
      imageName: localcontext/blahimage:${REVISION}
    ports:
    - "8080"

template:
  envSubst: true # use ENV vars during templating
